const PI = Matemática. PI 
Defina e exporte como funções circleArea() e squareArea() abaixo
círculo de funçãoArea (raio) {
		retorno PI * raio * raio
}
módulo. exportações. círculoArea = círculoArea;

módulo. exportações. quadradoArea = função (lado) {
		retorno lado * lado;
}