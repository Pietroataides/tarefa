const { circleArea, squareArea } = require ('./shape-area.js');

const raio = processo. argv[2];
const lado = processo. argv[3];

área ConstDeCircle = círculoArea (raio);
área ConstOfSquare = quadradoArea (lado);

console. log('A área do círculo ${raio} é ${areaRario.toFixed(2)}')
console. log('A área do quadrado ${lado} é ${areaLado.toFixed(2)}')